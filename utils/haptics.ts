/**
 * Disabled haptic feedback functions for web compatibility
 * These functions do nothing to prevent web errors
 */

export const safeHapticImpact = (style?: any) => {
  // Disabled for web compatibility
};

export const safeHapticNotification = (type?: any) => {
  // Disabled for web compatibility
};

export const safeHapticSelection = () => {
  // Disabled for web compatibility
};
